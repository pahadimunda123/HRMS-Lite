# For local development, the Vite proxy forwards /api to the backend.
# No need to set VITE_API_URL when running locally with proxy.

# For production build, set your deployed backend URL:
# VITE_API_URL=https://your-backend.onrender.com/api
